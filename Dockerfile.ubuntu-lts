# Dockerfile for Ubuntu LTS GNAT

# latest is actually latest LTS
FROM ubuntu:latest  

RUN apt-get update && \
    apt-get install -y \
        bash \
        git \
        gnat \
        gprbuild \
        lsb-release \
        mercurial \
        subversion && \
    apt-get clean
